---
phase: 02-data-pipeline
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - supabase/migrations/0004_cron_schedule.sql
  - .github/workflows/ci.yml
autonomous: false
requirements: [PIPE-05, PIPE-06]
user_setup:
  - service: supabase
    why: "Database schema must be pushed to live Supabase project"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "supabase.com -> Account -> Access Tokens -> Generate new token"
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Project Settings -> Database -> Database password"
    dashboard_config:
      - task: "Link local project to remote Supabase project"
        location: "Run: supabase link --project-ref YOUR_PROJECT_REF"
  - service: supabase-vault
    why: "pg_cron needs project URL and service role key in Vault for HTTP calls"
    dashboard_config:
      - task: "Store project URL in Vault"
        location: "Supabase Dashboard -> SQL Editor -> Run: select vault.create_secret('https://YOUR-PROJECT.supabase.co', 'project_url')"
      - task: "Store service role key in Vault"
        location: "Supabase Dashboard -> SQL Editor -> Run: select vault.create_secret('YOUR_SERVICE_ROLE_KEY', 'service_role_key')"
  - service: supabase-edge-functions
    why: "Deploy sync Edge Function and set secrets"
    dashboard_config:
      - task: "Deploy sync Edge Function"
        location: "Run: supabase functions deploy sync"
      - task: "Set Google service account secret"
        location: "Run: supabase secrets set GOOGLE_SERVICE_ACCOUNT_JSON='minified-json-here'"
      - task: "Set Gemini API key secret"
        location: "Run: supabase secrets set GEMINI_API_KEY='your-key-here'"
      - task: "Set sync secret for manual trigger"
        location: "Run: supabase secrets set SYNC_SECRET='a-strong-random-string'"
  - service: google-workspace
    why: "Service account with domain-wide delegation for calendar access"
    env_vars:
      - name: GOOGLE_SERVICE_ACCOUNT_JSON
        source: "Google Cloud Console -> IAM -> Service Accounts -> Create key (JSON)"
    dashboard_config:
      - task: "Create service account"
        location: "Google Cloud Console -> IAM & Admin -> Service Accounts -> Create"
      - task: "Enable Google Calendar API"
        location: "Google Cloud Console -> APIs & Services -> Library -> Google Calendar API -> Enable"
      - task: "Configure domain-wide delegation"
        location: "Google Workspace Admin -> Security -> API Controls -> Domain-wide Delegation -> Add client ID with scope https://www.googleapis.com/auth/calendar.readonly"

must_haves:
  truths:
    - "Database schema is pushed to live Supabase project and all tables exist"
    - "pg_cron job is scheduled to run nightly at 04:00 UTC (5 AM CET)"
    - "CI workflow validates Edge Function code on push/PR"
    - "All secrets are configured in Supabase Edge Functions"
    - "Sync function can be triggered manually and returns results"
  artifacts:
    - path: "supabase/migrations/0004_cron_schedule.sql"
      provides: "pg_cron + pg_net schedule for nightly sync"
      contains: "cron.schedule"
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI with Deno check for Edge Functions"
  key_links:
    - from: "supabase/migrations/0004_cron_schedule.sql"
      to: "supabase/functions/sync/index.ts"
      via: "pg_cron triggers HTTP POST to /functions/v1/sync"
      pattern: "functions/v1/sync"
---

<objective>
Push the database schema to Supabase, create the pg_cron nightly schedule, update CI to validate Edge Function code, and verify the full pipeline works end-to-end with a manual sync.

Purpose: This plan transitions from local development to a working deployed system. The database schema push is a BLOCKING prerequisite. The cron schedule enables the nightly automation (PIPE-05). The human verification confirms the system works.

Output: Deployed schema, cron schedule migration, updated CI, verified working pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@.planning/phases/02-data-pipeline/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pg_cron migration, update CI, and push schema to Supabase</name>
  <files>
    supabase/migrations/0004_cron_schedule.sql,
    .github/workflows/ci.yml
  </files>
  <read_first>
    supabase/migrations/0001_schema.sql,
    supabase/migrations/0002_seed.sql,
    supabase/migrations/0003_rls.sql,
    .github/workflows/ci.yml,
    .planning/phases/02-data-pipeline/02-RESEARCH.md
  </read_first>
  <action>
**Step 1: Create `supabase/migrations/0004_cron_schedule.sql`** (per D-13, PIPE-05)

This migration sets up the pg_cron nightly schedule using the pattern from Research Pattern 3. It uses pg_net to make an HTTP POST to the sync Edge Function and Supabase Vault for secure credential storage.

```sql
-- Schedule nightly calendar sync at 04:00 UTC (= 05:00 CET / 06:00 CEST)
-- Per D-13: ~5 AM CET. Using fixed UTC offset (Research Open Question #1: 1hr CEST drift acceptable).
-- Requires pg_cron and pg_net extensions to be enabled in Supabase Dashboard first.

-- Note: Vault secrets (project_url, service_role_key) must be created manually in SQL Editor
-- before this cron job can execute successfully. See user_setup.

SELECT cron.schedule(
  'nightly-calendar-sync',
  '0 4 * * *',
  $$
  SELECT net.http_post(
    url := (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'project_url' LIMIT 1)
           || '/functions/v1/sync',
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'Authorization', 'Bearer ' || (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'service_role_key' LIMIT 1)
    ),
    body := '{"trigger": "cron"}'::jsonb
  );
  $$
);
```

**Step 2: Update `.github/workflows/ci.yml`.**

Read the existing CI workflow. Add a Deno type-check step for Edge Function code after the existing npm test + build steps.

Add these steps to the existing job (after `npm run build`):

```yaml
- name: Install Deno
  uses: denoland/setup-deno@v2
  with:
    deno-version: v2.x

- name: Type-check Edge Functions
  run: deno check supabase/functions/sync/index.ts
```

Do NOT remove existing steps (npm ci, npm test, npm run build). Append the Deno steps.

**Step 3: [BLOCKING] Push schema to Supabase.**

This step pushes all migrations to the live Supabase project. Before running, the user must have:
1. Created the Supabase project
2. Linked the local project: `supabase link --project-ref YOUR_PROJECT_REF`
3. Enabled pg_cron and pg_net extensions in the dashboard

Push migrations:
```bash
supabase db push
```

If `supabase db push` requires interactive confirmation, set `SUPABASE_ACCESS_TOKEN` environment variable to bypass the prompt. If the push fails due to extensions not being enabled, the error message will indicate which extension is missing.

After push succeeds, verify tables exist:
```bash
supabase db inspect --linked
```

Or verify via the Supabase Dashboard SQL Editor:
```sql
SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' ORDER BY table_name;
```

Expected output: audit_log, categories, client_aliases, clients, events, switchers, sync_runs.

Note: The cron job (0004_cron_schedule.sql) will be included in the push but will not work until Vault secrets are created. This is expected -- the user_setup items for Vault secret creation are documented above.
  </action>
  <verify>
    <automated>
grep -q "cron.schedule" supabase/migrations/0004_cron_schedule.sql && echo "cron schedule found" || echo "FAIL";
grep -q "nightly-calendar-sync" supabase/migrations/0004_cron_schedule.sql && echo "job name found" || echo "FAIL";
grep -q "0 4" supabase/migrations/0004_cron_schedule.sql && echo "04:00 UTC schedule found" || echo "FAIL";
grep -q "functions/v1/sync" supabase/migrations/0004_cron_schedule.sql && echo "sync function URL found" || echo "FAIL";
grep -q "vault.decrypted_secrets" supabase/migrations/0004_cron_schedule.sql && echo "Vault usage found" || echo "FAIL";
grep -q "deno check" .github/workflows/ci.yml && echo "Deno check in CI" || echo "FAIL";
grep -q "setup-deno" .github/workflows/ci.yml && echo "Deno setup action found" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0004_cron_schedule.sql calls cron.schedule with job name 'nightly-calendar-sync'
    - 0004_cron_schedule.sql uses cron expression '0 4 * * *' (04:00 UTC)
    - 0004_cron_schedule.sql uses net.http_post to /functions/v1/sync
    - 0004_cron_schedule.sql reads project_url and service_role_key from vault.decrypted_secrets
    - 0004_cron_schedule.sql sends body '{"trigger": "cron"}'
    - .github/workflows/ci.yml contains denoland/setup-deno@v2 action
    - .github/workflows/ci.yml contains `deno check supabase/functions/sync/index.ts` step
    - .github/workflows/ci.yml still contains existing npm ci, npm test, npm run build steps
    - Database schema pushed successfully (all 7 tables exist in live Supabase project)
  </acceptance_criteria>
  <done>pg_cron nightly schedule created. CI updated with Deno type-checking. Schema pushed to live Supabase project.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify pipeline end-to-end</name>
  <files>N/A -- verification only</files>
  <action>
Human verification of the complete deployed pipeline. All code was built in Plans 01-04, schema pushed in Task 1 of this plan. This task verifies everything works end-to-end in the live environment.

Pre-requisites the user must complete:
1. Supabase project created and linked
2. Schema pushed (supabase db push)
3. Extensions enabled: pg_cron, pg_net
4. Vault secrets created (project_url, service_role_key)
5. Edge Function deployed (supabase functions deploy sync)
6. Edge Function secrets set (GOOGLE_SERVICE_ACCOUNT_JSON, GEMINI_API_KEY, SYNC_SECRET)
7. Google service account with domain-wide delegation and Calendar API enabled

Verification steps:
1. Check schema in Supabase Dashboard Table Editor -- 7 tables should exist
2. Check seed data -- 15 switchers, ~35 clients, ~35 categories
3. Trigger manual sync via curl POST to /functions/v1/sync with x-sync-secret header
4. Check curl response for "success" or "partial" status with events_processed > 0
5. Check events table for classified events with client_id, category_id, department, classification_method
6. Check sync_runs table for sync record with metrics
7. Check classification quality -- rule events should be ~98%, misc should be minimal
8. Verify no personal/weekend events in events table; Friday events have off_schedule = true
9. Check cron job in SQL Editor: SELECT * FROM cron.job WHERE jobname = 'nightly-calendar-sync'
  </action>
  <verify>Human visual inspection of Supabase Dashboard and curl response output.</verify>
  <done>Pipeline produces classified events from real Google Calendar data. Classification quality meets targets. Sync metrics recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL -> Supabase DB | Schema push modifies live database |
| pg_cron -> Edge Function | Scheduled HTTP call with auth credentials from Vault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Information Disclosure | 0004_cron_schedule.sql | mitigate | Credentials read from vault.decrypted_secrets, not hardcoded in SQL. Vault secrets created via SQL Editor (not in migration file). |
| T-02-15 | Elevation of Privilege | ci.yml | accept | CI runs Deno check (type verification only, no code execution). No secrets needed for type checking. |
</threat_model>

<verification>
- All 7 tables exist in live Supabase project
- Seed data present (15 switchers, ~35 clients, ~35 categories)
- pg_cron job scheduled at 04:00 UTC
- Manual sync returns classification results
- CI includes Deno type-check for Edge Functions
- Events table populated after manual sync
</verification>

<success_criteria>
- Schema deployed to Supabase with all tables, functions, indexes, and RLS
- Nightly cron job scheduled and visible in cron.job table
- Manual sync trigger produces classified events in the database
- Majority of events classified by rules (~98%), LLM handles remainder
- Event filtering works (no personal/weekend events in DB)
- Classification metrics recorded in sync_runs table
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-05-SUMMARY.md`
</output>
