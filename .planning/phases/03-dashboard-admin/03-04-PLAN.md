---
phase: 03-dashboard-admin
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/features/admin/AdminView.jsx
  - src/features/admin/AdminTable.jsx
  - src/features/admin/AdminModal.jsx
  - src/features/admin/SwitchersTab.jsx
  - src/features/admin/ClientsTab.jsx
  - src/features/admin/CategoriesTab.jsx
  - src/features/admin/BillingTab.jsx
  - src/features/admin/SyncTab.jsx
autonomous: false
requirements: [ADMN-01, ADMN-02, ADMN-03, ADMN-04, BILL-01]

must_haves:
  truths:
    - "Management user can add a new Switcher with name, email, primary_dept, is_management_member"
    - "Management user can add a new client with name, target_hourly_rate, and manage aliases"
    - "Management user can add a new category with name, department, llm_hint"
    - "Management user can enter a monthly billing amount for a client with EUR/USD currency and FX rate"
    - "Management user can trigger a manual sync and see live progress"
    - "All admin entities support soft-delete via active toggle with 'Show deactivated' toggle"
    - "Server-side validation errors surface as inline form messages"
  artifacts:
    - path: "src/features/admin/AdminView.jsx"
      provides: "Tab container for 5 admin sub-sections"
      contains: "Switchers"
    - path: "src/features/admin/AdminTable.jsx"
      provides: "Reusable table component for admin entities"
      contains: "AdminTable"
    - path: "src/features/admin/AdminModal.jsx"
      provides: "Reusable base modal for CRUD forms"
      contains: "AdminModal"
    - path: "src/features/admin/SwitchersTab.jsx"
      provides: "Switchers CRUD table + modal"
      contains: "SwitchersTab"
    - path: "src/features/admin/ClientsTab.jsx"
      provides: "Clients CRUD table + modal with aliases"
      contains: "ClientsTab"
    - path: "src/features/admin/CategoriesTab.jsx"
      provides: "Categories CRUD table + modal"
      contains: "CategoriesTab"
    - path: "src/features/admin/BillingTab.jsx"
      provides: "Billing entry table + modal with EUR/USD and FX rate"
      contains: "BillingTab"
    - path: "src/features/admin/SyncTab.jsx"
      provides: "Sync log table + sync-now button with progress"
      contains: "SyncTab"
  key_links:
    - from: "src/features/admin/SwitchersTab.jsx"
      to: "src/shared/services/adminApi.js"
      via: "import { adminApi }"
      pattern: "adminApi"
    - from: "src/features/admin/AdminView.jsx"
      to: "src/features/admin/SwitchersTab.jsx"
      via: "conditional tab rendering"
      pattern: "activeTab.*switchers"
---

<objective>
Build the complete Admin UI: tab container with 5 sub-sections (Switchers, Clients, Categories, Billing, Sync), shared AdminTable and AdminModal components, and all entity-specific CRUD tabs with forms.

Purpose: Fulfills ADMN-01 through ADMN-04 (reference data management without code changes) and BILL-01 (billing entry). The admin Edge Function from Plan 01 handles the server-side; this plan builds the full client-side.
Output: 8 new React components in `src/features/admin/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-admin/03-CONTEXT.md
@.planning/phases/03-dashboard-admin/03-RESEARCH.md
@.planning/phases/03-dashboard-admin/03-PATTERNS.md
@.planning/phases/03-dashboard-admin/03-UI-SPEC.md

@.planning/phases/03-dashboard-admin/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 -->
From src/shared/services/adminApi.js:
```javascript
export const adminApi = async (action, payload = {}) => { ... };
// Actions: create-switcher, update-switcher, create-client, update-client,
//          create-alias, delete-alias, create-category, update-category,
//          create-billing, update-billing, delete-billing, trigger-sync
// Throws Error with server's plain-English message on failure
```

From src/shared/hooks/useSyncStatus.js:
```javascript
export const useSyncStatus = (latestSync) => {
  return { syncState, progress, result, triggerSync, reset };
  // syncState: 'idle' | 'firing' | 'polling' | 'done' | 'error' | 'fallback'
  // triggerSync(adminApiFn): triggers sync via admin API
};
```

<!-- Existing component patterns -->
From src/shared/components/SettingsModal.jsx:
- if (!isOpen) return null; early return
- Overlay: fixed inset-0 bg-black/40 backdrop-blur-sm
- Container: bg-white rounded-2xl w-full max-w-md p-6 shadow-2xl animate-in

From src/shared/components/TaskTable.jsx:
- Table with sortable columns, hover rows, useMemo sorting

From src/shared/components/Card.jsx:
- bg-white rounded-2xl shadow-sm border border-stone-100 p-6
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminTable, AdminModal, and AdminView shell components</name>
  <files>src/features/admin/AdminTable.jsx, src/features/admin/AdminModal.jsx, src/features/admin/AdminView.jsx</files>
  <read_first>
    - src/shared/components/TaskTable.jsx (full file -- table structure pattern, sortable columns, hover rows)
    - src/shared/components/SettingsModal.jsx (full file -- modal pattern: overlay, container, form fields, footer)
    - src/shared/components/TaskDrilldownModal.jsx (close button pattern with X icon)
    - src/App.jsx (ListView component lines 21-75 for view shell pattern; navItems for routing context)
    - .planning/phases/03-dashboard-admin/03-UI-SPEC.md (Admin Table Pattern, Admin CRUD Modal Pattern, Admin Page Sub-Section Tabs, Copywriting Contract)
    - .planning/phases/03-dashboard-admin/03-PATTERNS.md (AdminTable analog, AdminModal analog, AdminView analog)
  </read_first>
  <action>
**File 1: `src/features/admin/AdminTable.jsx`** -- Shared table component for all admin entities.

Props: `columns` (array of { key, label, render? }), `data` (array), `onRowClick` (fn), `emptyMessage` (string), `showDeactivated` (bool), `onToggleDeactivated` (fn), `addLabel` (string), `onAdd` (fn).

Structure per UI-SPEC:
- Card wrapper: `bg-white rounded-2xl shadow-sm border border-stone-100 p-6`
- Header bar: flex row with "Show deactivated" toggle (left) and "Add" button (right)
  - "Show deactivated" toggle: `<label className="text-xs text-stone-400 hover:text-switch-secondary font-dm flex items-center gap-2 cursor-pointer"><input type="checkbox" ... /> Show deactivated</label>`
  - "Add" button: `bg-switch-secondary text-white text-sm font-bold px-4 py-2 rounded-lg hover:bg-switch-secondary-dark transition-colors flex items-center gap-2` with `Plus` icon (size 16)
- Table header: `text-xs font-bold uppercase tracking-wider text-stone-400 border-b border-stone-100`
- Header cells: `py-3 px-4 text-left`
- Body rows: `border-b border-stone-50 hover:bg-stone-50 transition-colors cursor-pointer`
- Body cells: `py-3 px-4 text-switch-secondary`
- Deactivated rows shown after a divider: `text-xs text-stone-400 font-dm py-2 border-t border-stone-100` "Deactivated" label
- Active badge: `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-bold bg-switch-bg text-switch-secondary`
- Inactive badge: `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-bold bg-stone-100 text-stone-400`
- Empty state: `py-16 text-center text-stone-400 text-sm font-dm` "{emptyMessage}"

**File 2: `src/features/admin/AdminModal.jsx`** -- Shared base modal component.

Props: `isOpen` (bool), `onClose` (fn), `title` (string), `children` (form content), `footer` (JSX -- action buttons), `error` (string -- server error message).

Structure per UI-SPEC:
- Early return: `if (!isOpen) return null;`
- Overlay: `fixed inset-0 bg-black/40 backdrop-blur-sm flex items-center justify-center z-50 p-4`
  - onClick on overlay calls onClose; inner container has `onClick={e => e.stopPropagation()}`
- Container: `bg-white rounded-2xl w-full max-w-lg p-6 shadow-2xl animate-in fade-in zoom-in-95 duration-200`
- Header: flex row with title + X close button
  - Title: `text-xl font-bold text-switch-secondary font-dm mb-6`
  - Close button: `text-stone-400 hover:text-stone-600` with X icon (size 24), `aria-label="Close"` (UI-SPEC accessibility requirement)
- Body: `{children}` (form fields provided by caller)
- Error display (when `error` prop is set): `text-xs text-red-500 font-dm mt-2 mb-2` prepended with "Couldn't save changes: "
- Footer: `flex justify-end gap-3 pt-4 border-t border-stone-100 mt-6` then `{footer}`
- Escape key close: `useEffect` with `keydown` listener for 'Escape' key

Form field helper styles (used by callers -- document in a comment at top):
```
label: block text-xs font-bold text-stone-500 uppercase tracking-wider mb-2
input: w-full p-3 border border-stone-200 rounded-xl focus:outline-none focus:border-switch-primary font-dm text-sm
select: same as input
field group: mb-4
validation error: text-xs text-red-500 font-dm mt-1
```

**File 3: `src/features/admin/AdminView.jsx`** -- Tab container shell.

Props: `refData`, `billingData`, `onDataChange`, `latestSync`, `initialTab` (string, default 'switchers').

Structure per UI-SPEC:
- Outer wrapper: `animate-in fade-in duration-500`
- Display heading: `text-3xl font-bold text-switch-secondary font-dm` "Admin"
- Tab strip: `bg-white rounded-xl border border-stone-100 p-1 flex gap-1 mb-6`
- 5 tabs (per D-05 order): Switchers (Users icon), Clients (Briefcase), Categories (Tag), Billing (Receipt), Sync (RefreshCw)
  - Active tab: `bg-switch-bg text-switch-secondary font-bold text-sm px-4 py-2 rounded-lg flex items-center gap-2`
  - Inactive tab: `text-stone-500 hover:bg-stone-50 hover:text-switch-secondary text-sm px-4 py-2 rounded-lg flex items-center gap-2`
- Content area: conditionally renders the matching tab component
- State: `const [activeTab, setActiveTab] = useState(initialTab || 'switchers');`
- useEffect to sync `initialTab` prop changes to activeTab

Import icons from lucide-react: Users, Briefcase, Tag, Receipt, RefreshCw.
  </action>
  <verify>
    <automated>grep -c "AdminTable" src/features/admin/AdminTable.jsx && grep -c "AdminModal" src/features/admin/AdminModal.jsx && grep -c "aria-label" src/features/admin/AdminModal.jsx && grep -c "AdminView" src/features/admin/AdminView.jsx && grep -c "activeTab" src/features/admin/AdminView.jsx && grep -c "switchers" src/features/admin/AdminView.jsx && grep -c "bg-switch-bg" src/features/admin/AdminView.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `AdminTable.jsx` exports `AdminTable` with columns, data, onRowClick, addLabel, onAdd props
    - `AdminTable.jsx` has "Show deactivated" checkbox toggle and "Add" button with Plus icon
    - `AdminTable.jsx` table headers: `text-xs font-bold uppercase tracking-wider text-stone-400`
    - `AdminTable.jsx` body rows: `border-b border-stone-50 hover:bg-stone-50 transition-colors cursor-pointer`
    - `AdminTable.jsx` active badge: `bg-switch-bg text-switch-secondary` / inactive: `bg-stone-100 text-stone-400`
    - `AdminModal.jsx` exports `AdminModal` with isOpen, onClose, title, children, footer, error props
    - `AdminModal.jsx` X button has `aria-label="Close"`
    - `AdminModal.jsx` handles click-outside (onClick on overlay) and Escape key
    - `AdminModal.jsx` error renders "Couldn't save changes: {error}"
    - `AdminView.jsx` exports `AdminView` with 5 tabs: Switchers, Clients, Categories, Billing, Sync
    - `AdminView.jsx` active tab styled `bg-switch-bg text-switch-secondary font-bold`
    - `AdminView.jsx` page heading is "Admin" in `text-3xl font-bold text-switch-secondary font-dm`
  </acceptance_criteria>
  <done>Three reusable admin components exist: AdminTable (shared table), AdminModal (shared modal), AdminView (tab container) with exact UI-SPEC styling.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 5 entity tabs (SwitchersTab, ClientsTab, CategoriesTab, BillingTab, SyncTab)</name>
  <files>src/features/admin/SwitchersTab.jsx, src/features/admin/ClientsTab.jsx, src/features/admin/CategoriesTab.jsx, src/features/admin/BillingTab.jsx, src/features/admin/SyncTab.jsx</files>
  <read_first>
    - src/features/admin/AdminTable.jsx (just created in Task 1 -- understand props interface)
    - src/features/admin/AdminModal.jsx (just created in Task 1 -- understand props interface)
    - src/shared/services/adminApi.js (adminApi function signature and available actions)
    - src/shared/hooks/useSyncStatus.js (useSyncStatus hook interface)
    - .planning/phases/03-dashboard-admin/03-UI-SPEC.md (ALL sections: Admin Table, Modal, Aliases, Billing Entry Modal, Sync Panel, Copywriting Contract)
    - .planning/phases/03-dashboard-admin/03-CONTEXT.md (D-06 table+modal, D-08 soft-delete, D-09 editable fields, D-10 sync-now in admin, D-11 validation, D-12 manual billing only, D-13 billing schema, D-16 EUR+USD, D-18 edit-freely billing, D-25 sync-now UX)
    - .planning/phases/03-dashboard-admin/03-RESEARCH.md (Pattern 5: Admin Edge Function actions, Server-Side Validation Examples section)
  </read_first>
  <action>
Create all 5 tab components. Each follows the same structure: use AdminTable for listing, AdminModal for create/edit, `adminApi` for mutations, `onDataChange` callback after success.

**File 1: `src/features/admin/SwitchersTab.jsx`** (per ADMN-01, D-09)

Props: `switchers` (array from refData.switchers), `onDataChange` (fn).

Columns: Name, Email, Department, Management, Status.
- Name: `row.name`
- Email: `row.email`
- Department: `row.primary_dept`
- Management: `row.is_management_member ? 'Yes' : '--'`
- Status: active badge or inactive badge

Modal fields (per D-09):
- name: text input, required
- email: text input, type="email", required
- primary_dept: select dropdown with options: Brand, Design, Digital, Film, Management, Production
- is_management_member: checkbox
- When editing existing with active=true, show "Deactivate" button in footer

Validation: on blur. Email format check client-side.

Save action: `adminApi(row.id ? 'update-switcher' : 'create-switcher', { ...formData })`.
Handle `requireConfirmation` response from server (deactivation warning): show warning text above action buttons and require a second click to confirm (send `{ ...payload, confirmed: true }`).

Modal save button: "Save Switcher" (per UI-SPEC Copywriting Contract).
Add button: "Add Switcher".
Empty message: "No Switchers found. Add the first one above."

**File 2: `src/features/admin/ClientsTab.jsx`** (per ADMN-02, D-09)

Props: `clients` (array), `aliases` (array), `onDataChange` (fn).

Columns: Name, Aliases, Target Rate, Status.
- Aliases: comma-joined from aliases array filtered by client_id
- Target Rate: `row.target_hourly_rate ? '€' + row.target_hourly_rate : '--'`

Modal fields:
- name: text input, required
- target_hourly_rate: number input (nullable, step 0.01)
- Aliases nested list per UI-SPEC: tag list with inline add/remove
  - Each alias tag: `inline-flex items-center gap-1 bg-stone-100 rounded-full px-3 py-1 text-xs font-dm text-switch-secondary mr-2 mb-2` with remove X button (`text-stone-400 hover:text-red-500`, X icon size 14)
  - "Add alias" inline input + "Add" button at end of tag list
  - Adding alias calls `adminApi('create-alias', { client_id, alias: value.toUpperCase() })`
  - Removing alias calls `adminApi('delete-alias', { id: aliasId })`

Save button: "Save Client". Add button: "Add Client".
Empty message: "No Clients found. Add the first one above."

**File 3: `src/features/admin/CategoriesTab.jsx`** (per ADMN-03, D-09)

Props: `categories` (array), `onDataChange` (fn).

Columns: Name, Department, LLM Hint, Status.

Modal fields:
- name: text input, required
- department: select dropdown (Brand, Design, Digital, Film, Management, Production)
- llm_hint: textarea (optional, 2 rows)

Save button: "Save Category". Add button: "Add Category".
Empty message: "No Categories found. Add the first one above."

**File 4: `src/features/admin/BillingTab.jsx`** (per BILL-01, D-12, D-13, D-16, D-18)

Props: `billingData` (array), `clients` (array), `onDataChange` (fn).

Columns: Client, Month, Amount, Currency, EUR Equivalent, Type, Entered By.
- Client: join to clients array by client_id to get name
- Month: format year_month as "MMM YYYY"
- Amount: formatted number
- EUR Equivalent: formatted EUR value

Modal fields per UI-SPEC Billing Entry Modal:
1. Client: `<select>` of active clients sorted A-Z
2. Month: `<input type="month">`
3. Amount: `<input type="number" step="0.01" min="0">` with currency prefix
4. Currency: inline pill toggle EUR | USD
   - Active: `bg-switch-secondary text-white px-3 py-1 rounded-full text-xs font-bold`
   - Inactive: `bg-stone-100 text-stone-500 px-3 py-1 rounded-full text-xs font-bold`
5. FX Rate (shown only when USD): `<input type="number" step="0.0001" min="0">`, label "FX rate to EUR (e.g. 0.92)", help text "Rate captured at time of entry -- not updated later."
6. EUR equivalent (computed, read-only when USD): show `= EUR{computed}` inline text
7. Billing type: `<select>` with "--" (null), Retainer, Project
8. Notes: `<textarea rows={2}>`
9. Entered by: `<input type="text">`

EUR equivalent auto-computes when amount or fx_rate changes: if EUR, eur_equivalent = amount; if USD, eur_equivalent = amount * fx_rate_to_eur.

Save action: `adminApi('create-billing', { client_id, year_month: `${month}-01`, amount, currency, fx_rate_to_eur, eur_equivalent, billing_type, notes, entered_by })`.

Delete: hard delete per D-18. Confirmation dialog: "Delete this entry? This billing record will be permanently removed. This cannot be undone." Button: "Delete Entry" (red-500 styled).

Save button: "Save Entry". Add button: "Add Entry".
Empty message: "No billing data entered yet for this client."

**File 5: `src/features/admin/SyncTab.jsx`** (per D-10, D-25, D-26)

Props: `latestSync`, `syncRuns` (from refData or separate query), `onDataChange` (fn).

Layout:
- "Sync Now" button: `bg-switch-secondary text-white text-sm font-bold px-5 py-2.5 rounded-lg hover:bg-switch-secondary-dark transition-colors flex items-center gap-2 min-h-[44px]` with RefreshCw icon. When syncing, RefreshCw gets `animate-spin` class.
- State machine per UI-SPEC: idle -> firing -> polling -> done/error/fallback
  - idle: "Sync Now", enabled
  - firing: "Starting sync...", disabled, spinning icon
  - polling: "Syncing...", disabled, progress text below "Syncing... {N} of 15 switchers done" or "Sync in progress -- this may take a few minutes"
  - done: Toast shown, button returns idle
  - error: Toast (red) "Sync failed -- check Admin > Sync log"
  - fallback (3 min): "Still running in background -- check back shortly", button returns idle
- Uses `useSyncStatus` hook from Plan 02 for state management
- Uses `adminApi` from Plan 02 -- passes adminApi to `triggerSync(adminApiFn)` 

Sync log table below button:
Columns: Date/Time, Status, Duration, Events Updated, Errors.
- Load recent sync_runs (e.g., last 20) from Supabase on mount
- Status badges: completed (green bg), failed (red bg), running (amber bg)
  </action>
  <verify>
    <automated>grep -c "SwitchersTab" src/features/admin/SwitchersTab.jsx && grep -c "create-switcher" src/features/admin/SwitchersTab.jsx && grep -c "Save Switcher" src/features/admin/SwitchersTab.jsx && grep -c "ClientsTab" src/features/admin/ClientsTab.jsx && grep -c "create-alias" src/features/admin/ClientsTab.jsx && grep -c "Save Client" src/features/admin/ClientsTab.jsx && grep -c "CategoriesTab" src/features/admin/CategoriesTab.jsx && grep -c "Save Category" src/features/admin/CategoriesTab.jsx && grep -c "BillingTab" src/features/admin/BillingTab.jsx && grep -c "fx_rate_to_eur" src/features/admin/BillingTab.jsx && grep -c "Save Entry" src/features/admin/BillingTab.jsx && grep -c "SyncTab" src/features/admin/SyncTab.jsx && grep -c "Sync Now" src/features/admin/SyncTab.jsx && grep -c "trigger-sync" src/features/admin/SyncTab.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `SwitchersTab.jsx` calls `adminApi('create-switcher', ...)` and `adminApi('update-switcher', ...)` with fields: name, email, primary_dept, is_management_member
    - `SwitchersTab.jsx` handles `requireConfirmation` response for deactivation warning
    - `SwitchersTab.jsx` save button says "Save Switcher"
    - `ClientsTab.jsx` renders alias tag list with add/remove functionality
    - `ClientsTab.jsx` calls `adminApi('create-alias', { client_id, alias })` and `adminApi('delete-alias', { id })`
    - `ClientsTab.jsx` save button says "Save Client"
    - `CategoriesTab.jsx` has department select with 6 options: Brand, Design, Digital, Film, Management, Production
    - `CategoriesTab.jsx` save button says "Save Category"
    - `BillingTab.jsx` has currency toggle between EUR and USD
    - `BillingTab.jsx` shows FX rate field only when currency is USD
    - `BillingTab.jsx` computes eur_equivalent: amount when EUR, amount * fx_rate_to_eur when USD
    - `BillingTab.jsx` has hard delete with confirmation: "Delete this entry?" / "Delete Entry" button
    - `BillingTab.jsx` save button says "Save Entry"
    - `SyncTab.jsx` has "Sync Now" button that triggers `adminApi('trigger-sync')`
    - `SyncTab.jsx` uses `useSyncStatus` hook for state management
    - `SyncTab.jsx` shows sync log table with columns: Date/Time, Status, Duration, Events, Errors
    - All tabs call `onDataChange()` after successful mutations
    - All tabs use `AdminTable` for listing and `AdminModal` for forms
  </acceptance_criteria>
  <done>All 5 admin tabs fully functional with CRUD operations, validation, deactivation support, and correct UI-SPEC styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Admin UI</name>
  <files>src/features/admin/AdminView.jsx</files>
  <action>Human verifies Admin UI: run npm run dev, navigate to Admin, check all 5 tabs render with correct CRUD modals and styling per UI-SPEC.</action>
  <verify>
    <automated>echo "CHECKPOINT: Manual verification required"</automated>
  </verify>
  <done>User confirms Admin UI works: 5 tabs visible, modals open with correct fields, save buttons entity-keyed, aria-label on close buttons.</done>
  <what-built>Complete Admin UI with 5 tabs: Switchers, Clients (with aliases), Categories, Billing (EUR/USD with FX rate), and Sync (sync-now with progress). All CRUD operations go through the admin Edge Function.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in the browser
    2. Navigate to Admin in the sidebar
    3. Verify 5 tabs appear: Switchers, Clients, Categories, Billing, Sync
    4. Click "Add Switcher" -- verify modal opens with fields: Name, Email, Department dropdown, Management checkbox
    5. Click a client row -- verify edit modal opens with alias tag list
    6. Go to Billing tab -- verify currency toggle between EUR/USD; when USD selected, FX rate field appears
    7. Go to Sync tab -- verify "Sync Now" button is present
    8. Verify all modals have aria-label="Close" on X button and close on Escape key
    9. Verify save buttons say "Save Switcher", "Save Client", "Save Category", "Save Entry" (not generic "Save")
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser admin forms -> Admin Edge Function | All mutations go through server-side validation; browser does optimistic client-side checks only |
| Admin Edge Function -> Supabase | Service role key; function validates all inputs before querying |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | Admin form inputs | mitigate | Client-side validation on blur + server-side validation in Edge Function; server is source of truth |
| T-03-13 | Repudiation | Billing CRUD | accept | Track updated_at per D-18; no immutable audit log (trusted 6-person team) |
| T-03-14 | Information Disclosure | Admin tables | accept | All admin users see same data; no per-user scoping needed |
</threat_model>

<verification>
1. All 8 admin component files exist in src/features/admin/
2. AdminView renders 5 tabs with correct labels and icons
3. Each tab uses AdminTable + AdminModal pattern
4. Each tab calls adminApi with correct action names
5. Billing tab handles EUR/USD toggle and FX rate conditional display
6. Sync tab integrates with useSyncStatus hook
7. npm run dev shows Admin UI without console errors
</verification>

<success_criteria>
- All 5 admin sub-sections render and allow CRUD operations
- Entity-specific save buttons per Copywriting Contract
- Soft delete with "Show deactivated" toggle works
- Billing supports EUR and USD with FX rate entry
- Sync-now button triggers and shows progress
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-admin/03-04-SUMMARY.md`
</output>
